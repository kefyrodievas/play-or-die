extends Node2D


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass
	#var map_limits = $TileMapLayer.get_used_rect()
	#var map_cellsize = $TileMapLayer.
	#$Samurai/Camera2D.limit_left = map_limits.position.x * map_cellsize.x
	#$Samurai/Camera2D.limit_right = map_limits.end.x * map_cellsize.x
	#$Samurai/Camera2D.limit_top = map_limits.position.y * map_cellsize.y
	#$Samurai/Camera2D.limit_bottom = map_limits.end.y * map_cellsize.y
	#


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
